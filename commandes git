Commandes à exécuter pour envoyer :
git add . --> Ajoute le dossier
git remote add origin https://github.com/schmuckl/projetPHP-tp5.git
git commit -m "MESSAGE" --> Commit avec un message personnalisable
git push origin master --> Push le répertoire


Pour récupérer les fichiers :
git clone LIEN A RECUPERER (une fois)
git pull
